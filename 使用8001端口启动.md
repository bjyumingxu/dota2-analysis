# 🚀 使用8001端口启动（解决端口占用问题）

## ✅ 我已经完成
- ✅ 修改了前端代理配置，指向8001端口
- ✅ 8001端口目前未被占用

## 🚀 现在请执行以下步骤

### 步骤1：停止前端服务（如果正在运行）

找到运行前端的终端窗口，按 `Ctrl+C` 停止。

### 步骤2：重新启动前端（使用新的代理配置）

```powershell
cd D:\cursorPRO\opendotaAI\frontend
npm run dev
```

**重要**：必须重启前端，因为 `vite.config.ts` 已修改！

### 步骤3：启动后端（使用8001端口）

**打开一个新的PowerShell或CMD窗口**，执行：

```powershell
cd D:\cursorPRO\opendotaAI\backend
.\venv\Scripts\Activate.ps1
uvicorn src.main:app --reload --host 0.0.0.0 --port 8001
```

**启动时应该看到**：
```
INFO: 🚀 FastAPI应用启动完成，日志系统已初始化
INFO:     Uvicorn running on http://0.0.0.0:8001
INFO:     Application startup complete.
```

### 步骤4：测试健康检查

在浏览器访问：http://localhost:8001/health

**后端终端应该立即显示**：
```
📥 收到请求: GET /health
💚 健康检查被调用 - print输出
📤 响应: 200 (耗时: 0.xx秒)
```

### 步骤5：测试前端

1. 访问：http://localhost:5173
2. 查询一个账号
3. **后端终端应该立即显示大量日志**

## 🔍 如果还是看不到日志

### 检查1：确认后端真的在8001端口运行

访问：http://localhost:8001/health

应该返回：`{"status":"ok"}`

如果访问不了，说明后端没有启动成功。

### 检查2：确认前端代理配置已更新

打开 `frontend/vite.config.ts`，确认：
```typescript
target: 'http://localhost:8001',
```

### 检查3：确认前端已重启

前端服务必须重启才能使用新的代理配置！

## 📝 请告诉我

1. **访问 http://localhost:8001/health 时，后端终端有没有日志输出？**
   - 应该看到 `📥 收到请求` 和 `💚 健康检查被调用`

2. **查询账号时，后端终端有没有日志输出？**
   - 应该看到很多行日志

3. **前端能正常查询到数据吗？**

